<!DOCTYPE html>
<html>
<head>
  <title>Jamtastic API</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <style>
    body {
      margin: 0;
      padding: 0;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: linear-gradient(135deg, <%= @background_color %> 0%, <%= @secondary_color %> 100%);
      transition: background 0.5s ease;
    }
    
    .logo-container {
      text-align: center;
      padding: 2rem;
      background: rgba(255, 255, 255, 0.1);
      border-radius: 20px;
      backdrop-filter: blur(10px);
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
    }
    
    .logo {
      max-width: 300px;
      height: auto;
      filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.3));
    }
    
    .api-info {
      margin-top: 2rem;
      color: white;
      text-shadow: 0 2px 4px rgba(0, 0, 0, 0.5);
    }
    
    .api-info h1 {
      margin: 0 0 0.5rem 0;
      font-size: 2rem;
      font-weight: 300;
    }
    
    .api-info p {
      margin: 0;
      opacity: 0.8;
      font-size: 1.1rem;
    }
  </style>
</head>
<body>
  <div class="logo-container">
    <img src="/logos/jamtastic-white-logo.png" alt="Jamtastic" class="logo">
    <div class="api-info">
      <h1>Jamtastic API</h1>
      <p>RESTful API Server</p>
    </div>
  </div>
</body>
</html>
